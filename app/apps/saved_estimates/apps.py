from django.apps import AppConfig


class SavedEstimatesConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.saved_estimates'
    verbose_name = 'Saved estimates'

