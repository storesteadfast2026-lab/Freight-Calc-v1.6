# Legacy compatibility pointer — validation baseline pairing

This filename previously collided with canonical ADR 0002
(`random_current`). The canonical baseline-pairing decision is:

```text
docs/adr/0008_validation_baseline_pairing.md
```

This file is retained only so historical references do not break. It is not a
second ADR and must not contain an independent decision.
